# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
storybook-static/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store

# Expo
.expo/
expo-env.d.ts

# IDE
.vscode/
.idea/

# Testing
coverage/

# Supabase
supabase/.temp/

# Temporary accommodation: SYNTH CLI v2 runtime artifacts.
# These are tool-generated outputs that should eventually live outside the
# governed repository (e.g., user cache or application data directory).
# Revisit once the CLI's output behavior is stable.
data/
event-log.jsonl

# Ephemeral CLI documentation projection. Ignore only while it remains an
# unreviewed, automatically generated artifact. Revisit if it becomes a
# governed documentation projection.
docs/generated/
