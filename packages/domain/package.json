{
  "name": "@taskpro/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Backend-independent domain layer for TaskPRO",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@taskpro/types": "workspace:*"
  },
  "devDependencies": {
    "@taskpro/config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
