{
  "name": "@taskpro/api",
  "version": "0.0.0",
  "private": true,
  "description": "TaskPRO API — Supabase Edge Functions, webhooks, and provider adapters",
  "scripts": {
    "build": "echo 'API build configured in later expedition'",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@taskpro/config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
