{
  "name": "@taskpro/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "TaskPRO Expo React Native mobile application",
  "scripts": {
    "dev": "expo start",
    "build": "echo 'Mobile build configured in later expedition'",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@taskpro/config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
